<img src="/assets/img/burger.jpeg" alt="Burger Image" width="400" height="300">

<h1>Eat-Da-Burger!</h1>

{{!-- burgers to be eaten --}}
<div>
  <p>Burgers to be Eaten!</p>
<ul>
{{#each burgers}}
  <span>{{burger_name}}</span>
  <button class="changeEatStatus" data-id="{{id}}" data-devoured="{{devoured}}">
	Devour!
	</button>
  <br>
{{/each}}
</ul>
</div>


{{!-- burgers already eaten --}}
<div id="devoured-div">
<p>Burgers Eaten:</p>

</div>

{{!-- input txt box --}}
<form class="create-form">
  <div class="form-group">
    <input type="text" id="burger-name" name="burger">
    <button type="submit">Summit</button>
  </div>
</form>

